Initials (or image) avatar with navy fill — deal owners, contacts, account menu.

```jsx
<Avatar name="Иван Петров" />
<Avatar name="MG CRM Admin" size="lg" />
<Avatar src="/photo.jpg" name="Анна" />
```

Props: `name` (→ initials), `src`, `size` xs/sm/md/lg or px number, `color`, `square`.
