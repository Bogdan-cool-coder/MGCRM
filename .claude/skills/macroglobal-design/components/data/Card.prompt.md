White surface container — 1px border, 8px radius, soft card shadow, optional header with icon + actions.

```jsx
<Card title="Сводка" icon="pi-chart-bar" actions={<Button text icon="pi-ellipsis-h" />}>
  …
</Card>
```

Props: `title`, `icon`, `actions`, `padding`, `hover` (lift shadow).
