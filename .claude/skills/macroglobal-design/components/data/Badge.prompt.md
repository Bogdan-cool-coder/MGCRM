Count indicator for nav items and tab headers — amber by default, red `danger`, matching the sidebar badges.

```jsx
<Badge value={20} />
<Badge value={3} variant="danger" />
<Badge dot variant="danger" />
```

Props: `value`, `variant` warning/danger/primary, `dot` (bare indicator).
