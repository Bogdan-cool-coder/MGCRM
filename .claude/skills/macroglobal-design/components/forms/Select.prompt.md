Closed-state dropdown trigger mirroring PrimeVue Select — for specimens and prototypes.

```jsx
<Select placeholder="Ответственный" />
<Select value="Москва" open />
```

Props: `value`, `placeholder`, `size`, `open` (focused border + ring), `disabled`, `fullWidth`.
