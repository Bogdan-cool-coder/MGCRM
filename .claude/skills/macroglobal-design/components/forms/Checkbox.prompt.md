Checkbox matching PrimeVue — navy fill when checked, optional inline label.

```jsx
<Checkbox checked label="Только мои сделки" onChange={setOn} />
```

Props: `checked`, `label`, `disabled`, `onChange(checked)`.
