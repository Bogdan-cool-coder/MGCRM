import { appTheme } from '@/theme/config'

export const primeVueColors = {
  primary: appTheme.palette.primary,
  surface: appTheme.palette.surface,
  red: appTheme.palette.red,
  orange: appTheme.palette.orange,
  blue: appTheme.palette.blue,
  green: appTheme.palette.green,
  monochrome: appTheme.palette.monochrome,
} as const
