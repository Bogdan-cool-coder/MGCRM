# MGCRM — backend (Laravel 13 API)

Laravel 13 API для MACRO Global CRM. Контекст и правила — в корне репозитория: `/CLAUDE.md`, `/ARCHITECTURE.md` (закон проекта), `/PLAN.md` (план миграции).

Стек **закрыт** (PLAN §3): не добавлять `laravel/boost` и любые пакеты вне списка без явной просьбы + аппрува.
