networks:
  macro-crm-network:
    driver: bridge
  # External Traefik network — name is server-specific, set TRAEFIK_NETWORK in root .env.
  # On the production VPS the network is called "proxy" (created by Traefik).
  proxy:
    name: ${TRAEFIK_NETWORK:-proxy}
    external: true

services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
      target: app
    image: macro-crm-app:${IMAGE_TAG:-dev-latest}
    container_name: ${COMPOSE_PROJECT_NAME:-macro-crm}-app
    restart: unless-stopped
    mem_limit: 512m
    cpus: 0.75
    env_file:
      - src/.env
    environment:
      GOTENBERG_URL: http://gotenberg:3000
    volumes:
      # Named volumes for runtime-writable directories (survive container recreate)
      - storage_app:/var/www/html/storage/app
      - storage_framework:/var/www/html/storage/framework
      - storage_logs:/var/www/html/storage/logs
      - bootstrap_cache:/var/www/html/bootstrap/cache
      # Backups target (bind to host path so backups survive image updates)
      - ./data/backups:/var/www/backups
      # Share public/ with nginx so nginx can serve favicon.ico, robots.txt etc.
      - app_public:/var/www/html/public
    networks:
      - macro-crm-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  # ---------------------------------------------------------------------------
  # nginx — serves Laravel public/ static assets + proxies PHP-FPM (app:9000).
  # Internal to macro-crm-network ONLY; NOT exposed to host.
  # Traefik never reaches nginx directly — traffic flows:
  #   internet → Traefik → frontend:80 → nginx:80 → app:9000 (php-fpm)
  # No host port published: eliminates conflict with port 8080 (amo-assistant-bot).
  # ---------------------------------------------------------------------------
  nginx:
    image: nginx:alpine
    container_name: ${COMPOSE_PROJECT_NAME:-macro-crm}-nginx
    restart: unless-stopped
    mem_limit: 96m
    cpus: 0.25
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      # Read-only access to public/ for favicon, robots.txt (no src bind mount)
      - app_public:/var/www/html/public:ro
    networks:
      - macro-crm-network
    depends_on:
      - app

  # ---------------------------------------------------------------------------
  # frontend — nginx:alpine serving the compiled Vue 3 SPA.
  #   - Serves /usr/share/nginx/html (Vite dist) with history-mode fallback.
  #   - Proxies /api/* → nginx:80 (backend nginx → php-fpm).
  #   - Exposed to Traefik via the external proxy network (no host port published).
  #   - HTTP→HTTPS redirect is handled globally by Traefik entrypoint args;
  #     we ONLY define the websecure router here to avoid conflicting middleware.
  # ---------------------------------------------------------------------------
  frontend:
    build:
      context: .
      dockerfile: docker/frontend/Dockerfile
      target: production
    image: macro-crm-frontend:${IMAGE_TAG:-dev-latest}
    container_name: ${COMPOSE_PROJECT_NAME:-macro-crm}-frontend
    restart: unless-stopped
    mem_limit: 96m
    cpus: 0.25
    networks:
      - macro-crm-network
      - proxy
    depends_on:
      - nginx
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.macro-crm.rule=Host(`${APP_DOMAIN:-mgcrm.macroglobal.tech}`)"
      - "traefik.http.routers.macro-crm.entrypoints=websecure"
      - "traefik.http.routers.macro-crm.tls=true"
      - "traefik.http.routers.macro-crm.tls.certresolver=${TRAEFIK_CERTRESOLVER:-letsencrypt}"
      - "traefik.http.services.macro-crm.loadbalancer.server.port=80"
      - "traefik.docker.network=${TRAEFIK_NETWORK:-proxy}"

  queue-worker:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
      target: app
    image: macro-crm-app:${IMAGE_TAG:-dev-latest}
    container_name: ${COMPOSE_PROJECT_NAME:-macro-crm}-queue-worker
    # Queue priority: default first (user-facing jobs) then automation (background scans).
    # Both queues are served by a single worker — no starvation risk at current load.
    # If automation jobs grow significantly, extract a dedicated automation-worker service.
    command: php artisan queue:work --queue=default,automation --tries=1 --timeout=600 --max-jobs=50 --memory=256 --sleep=1
    restart: unless-stopped
    mem_limit: 256m
    cpus: 0.5
    env_file:
      - src/.env
    environment:
      GOTENBERG_URL: http://gotenberg:3000
    volumes:
      # Same named volumes as app — worker shares storage/logs, storage/framework/cache
      - storage_app:/var/www/html/storage/app
      - storage_framework:/var/www/html/storage/framework
      - storage_logs:/var/www/html/storage/logs
      - bootstrap_cache:/var/www/html/bootstrap/cache
      - ./data/backups:/var/www/backups
    networks:
      - macro-crm-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      app:
        condition: service_started

  # ---------------------------------------------------------------------------
  # Scheduler — runs `php artisan schedule:work` to fire all registered crons.
  #
  # M7 automation commands that run hourly via this service:
  #   automation:scan-idle        — triggers idle-deal automations
  #   automation:scan-date-field  — triggers date-field automations
  #
  # SINGLE process: schedule:work polls every 60s internally. DO NOT scale.
  # ---------------------------------------------------------------------------
  scheduler:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
      target: app
    image: macro-crm-app:${IMAGE_TAG:-dev-latest}
    container_name: ${COMPOSE_PROJECT_NAME:-macro-crm}-scheduler
    command: php artisan schedule:work
    restart: unless-stopped
    mem_limit: 128m
    cpus: 0.25
    env_file:
      - src/.env
    environment:
      GOTENBERG_URL: http://gotenberg:3000
    volumes:
      - storage_app:/var/www/html/storage/app
      - storage_framework:/var/www/html/storage/framework
      - storage_logs:/var/www/html/storage/logs
      - bootstrap_cache:/var/www/html/bootstrap/cache
      - ./data/backups:/var/www/backups
    networks:
      - macro-crm-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      app:
        condition: service_started

  # ---------------------------------------------------------------------------
  # Telegram bot — long-polling in a SINGLE process.
  #
  # CRITICAL: replicas:1 — this service must NEVER be scaled. A parallel
  # getUpdates from a second process triggers Telegram 409 Conflict and drops
  # updates. RUN_TELEGRAM_POLLING=true is set ONLY here; `app` and `queue-worker`
  # do not poll (they resolve the same bot singleton purely as an outgoing Bot
  # API client). Deploy note: this service restarts on its OWN — it is NOT part
  # of the rolling restart of the web/queue replicas:
  #   docker compose up -d --force-recreate bot
  # ---------------------------------------------------------------------------
  bot:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
      target: app
    image: macro-crm-app:${IMAGE_TAG:-dev-latest}
    container_name: ${COMPOSE_PROJECT_NAME:-macro-crm}-bot
    command: php artisan nutgram:run
    restart: unless-stopped
    mem_limit: 128m
    cpus: 0.25
    env_file:
      - src/.env
    environment:
      RUN_TELEGRAM_POLLING: "true"
    volumes:
      - storage_app:/var/www/html/storage/app
      - storage_framework:/var/www/html/storage/framework
      - storage_logs:/var/www/html/storage/logs
      - bootstrap_cache:/var/www/html/bootstrap/cache
    networks:
      - macro-crm-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      app:
        condition: service_started

  # ---------------------------------------------------------------------------
  # SalesPulse bot — long-polling in a SINGLE process, on its OWN token.
  #
  # CRITICAL: replicas:1 — never scale. SalesPulse runs on SALESPULSE_BOT_TOKEN,
  # a DIFFERENT token than the contract bot, so the two getUpdates streams are
  # independent (they never 409 each other). But the per-token invariant holds:
  # exactly ONE process may poll the SalesPulse token. SALESPULSE_RUN_POLLING=true
  # is set ONLY here; web/queue resolve the singleton purely as an outbound client
  # (SalesPulseNotifier). Empty token → FakeNutgram fallback (idle, no crash-loop).
  # Deploy note: restarts on its OWN, NOT part of the web/queue rolling restart:
  #   docker compose up -d --force-recreate salespulse-bot
  # ---------------------------------------------------------------------------
  salespulse-bot:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
      target: app
    image: macro-crm-app:${IMAGE_TAG:-dev-latest}
    container_name: ${COMPOSE_PROJECT_NAME:-macro-crm}-salespulse-bot
    command: php artisan salespulse:run
    restart: unless-stopped
    mem_limit: 128m
    cpus: 0.25
    env_file:
      - src/.env
    environment:
      SALESPULSE_RUN_POLLING: "true"
    volumes:
      - storage_app:/var/www/html/storage/app
      - storage_framework:/var/www/html/storage/framework
      - storage_logs:/var/www/html/storage/logs
      - bootstrap_cache:/var/www/html/bootstrap/cache
    networks:
      - macro-crm-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      app:
        condition: service_started

  gotenberg:
    image: gotenberg/gotenberg:8
    container_name: ${COMPOSE_PROJECT_NAME:-macro-crm}-gotenberg
    restart: unless-stopped
    mem_limit: 256m
    cpus: 0.5
    networks:
      - macro-crm-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  postgres:
    image: postgres:16-alpine
    container_name: ${COMPOSE_PROJECT_NAME:-macro-crm}-postgres
    restart: unless-stopped
    mem_limit: 512m
    cpus: 0.75
    # No host port published — postgres is internal to macro-crm-network only.
    # For one-off admin access use: docker compose exec postgres psql -U macro_crm
    environment:
      POSTGRES_DB: ${DB_DATABASE:-macro_crm}
      POSTGRES_USER: ${DB_USERNAME:-macro_crm}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - macro-crm-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-macro_crm} -d ${DB_DATABASE:-macro_crm}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  redis:
    image: redis:7-alpine
    container_name: ${COMPOSE_PROJECT_NAME:-macro-crm}-redis
    restart: unless-stopped
    mem_limit: 128m
    cpus: 0.25
    command: redis-server --appendonly yes
    # No host port published — redis is internal to macro-crm-network only.
    volumes:
      - redis_data:/data
    networks:
      - macro-crm-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 5s

volumes:
  postgres_data:
  redis_data:
  storage_app:
  storage_framework:
  storage_logs:
  bootstrap_cache:
  app_public:
