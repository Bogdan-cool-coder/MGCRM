"""Финансовый модуль (double-entry GL). Ф0 — фундамент данных.

Single source of truth для сид-данных Ф0 (план счетов 39, дерево статей ДДС 39,
op-types, ставки НДС, дефолты прав ролей) живёт в `seed_data.py` — его читают
И Alembic-миграции, И pure-function тесты, чтобы сиды и проверки не разъезжались.

В этом чанке (ЧАНК 1) НЕТ posting engine / fx / balance / API — только данные
и чистые предикаты-валидаторы (НДС, маппинг юрлица). Остальное — следующие чанки.
"""
