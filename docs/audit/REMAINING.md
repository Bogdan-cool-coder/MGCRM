# MGCRM — Остаток после авто-фикса аудита (2026-06-24)

Закрыто за 3 волны: **161** проблем (17 P0 + ~80 major + ~60 minor/trivial), запушено в main (04dfa8c, b092670, bdc59d3, bd7f966). Backend 2954 тестов зелёные, vue-tsc 0, e2e 7 замков зелёные.

Осознанно **отложено: 124** пунктов — требуют продуктовых решений, отдельной инфры или выходят за рамки точечного багфикса. Список ниже (источник — отчёты волн).

## Крупные (требуют твоего решения)
- **IAM-1 — миграция RBAC на spatie.** Решение «оживить spatie» принято, но сама миграция (config/sanctum.php + auth-guard + перенос Gate→permissions) — отдельный трек. Сейчас авторизация работает на role-enum Gates (безопасно, дыры закрыты), spatie-таблицы засеяны, но не подключены.
- **ResolveVisibility middleware** — теперь вестигиальна (scope применяется в сервисах). Либо подключить её к реальному scope, либо удалить — мелкое решение по чистоте.
- **Department CRUD + орг-дерево + Department-visibility** — намеренный M0-скелет; это спринт M1, не баг.
- **Invite/email-флоу для новых юзеров + password-reset** — нужна mail-инфра + публичные роуты. Сейчас админ задаёт стартовый пароль вручную (закрыто частично).
- **Дроп скелет-таблиц (sessions / password_reset_tokens) и TTL Sanctum-токенов** — деструктив/дизайн-решение, не трогал без подтверждения.

## Полный список отложенного по доменам

### CRM blockers: cont
- **contacts-frontend-switch-bug** (Wave 1 (P0)) — ContactsPage/index.vue:792 has 'contact':'company' ternary bug — both branches return 'company'. This is a frontend file; fixing requires frontend-specialist. Not in crm-specialist file ownership scope.
- **created_by_id-null** (Wave 1 (P0)) — ContactService::create() doesn't set created_by_id; all 3 live contacts have NULL. Author filter and sort are no-ops. Minor/not P0 — audit classified as major BUG but not a security blocker.
- **contact-channel-idor** (Wave 1 (P0)) — channels/{channel} and relations/{relation} nested routes not scoped() — IDOR form. Routes are in src/routes/api.php which is owned by Foundation agent. Foundation must add ->scoped() to the route definitions.

### Contracts domain —
- **MAJOR 1 — Fake-approved demo documents bypass state machine (won-gate)** (Wave 1 (P0)) — DemoDealsSeeder creates approved docs with NULL docx_path to satisfy won-gate. Fixing requires either changing how demo data is seeded or strengthening hasActiveContractForDeal to require docx_path IS NOT NULL. Deferred: cross-concern with DemoDealsSeeder which is outside Contracts domain ownership.
- **MAJOR 3 — attempt double-increment in generation vs submit** (Wave 1 (P0)) — Requires schema change (new column generation_seq vs approval_attempt) or behavior change to not bump attempt on generate. No live impact currently since approvals=0, deferred to avoid risky schema change in this wave.
- **MAJOR 4-5 — MyApprovals History empty / undefined columns** (Wave 1 (P0)) — Backend fix needed (status='decided' mapper, ApprovalResource enrichment with document_number/kind/company_name). No live approvals data yet, deferred to next wave.
- **MAJOR 6-10 — Field name mismatches (items/revisions/remarks FE↔BE), currency not saved, bank account per-currency** (Wave 1 (P0)) — Frontend-only field renames and one FE emit fix. Substantial FE changes owned by frontend-specialist for the DocumentPage UI; deferred to coordinated UI wave.

### iam
- **MAJOR-1** (Wave 2 (major)) — RBAC drift (spatie permission layer dead, guard=web vs Sanctum). Fix requires editing config/sanctum.php, config/auth.php, AppServiceProvider Gate definitions — all out of my file-ownership scope (config/providers). This is the IAM-1 debt; needs a dedicated shared-file pass. New authz I added stays on the existing admin-write Gate (Policy/Gate path), not inline role checks.
- **NEW-5** (Wave 2 (major)) — /api/admin/* directories readable by manager role. Already FIXED at route level: the admin route group in routes/api.php carries middleware('can:admin-write') (verified at lines ~388). Root cause (single permission gate) is MAJOR-1. No further action in my scope.
- **BLOCKER-1** (Wave 2 (major)) — Login/2fa throttle. Per task brief this was closed in Wave 1; confirmed AuthService/login is the P0 already handled. Throttle wiring lives in routes/api.php + bootstrap/app.php + AppServiceProvider (out of scope).
- **minor-2fa-disable-regenerate** (Wave 2 (major)) — No disable-2FA / regenerate-backup-codes / admin-reset endpoints. Self-lockout risk is real but this is a new feature area (TwoFactorController is Auth/ not the user-mgmt scope listed) needing new routes; deferred as separate task to keep this pass surgical.
- **minor-invite-email-flow** (Wave 2 (major)) — Full invite / signed-link / password-reset email flow for new users. Needs mail infra + new public routes + password_reset_tokens usage — larger feature beyond the surgical user-mgmt fix. Mitigated for now: admin can set initial password in the create/edit dialog (MAJOR-6).

### crm-contacts
- **MAJOR-6** (Wave 2 (major)) — UI role gating (export/bulk/delete controls visible to all roles in contacts page) — the dangerous path (export) is already BE-guarded via authorize+scope; bulk update/delete is per-contact 403. Full FE role gating requires useAuthStore integration across ContactsToolbar, ContactsBulkToolbar, ContactPage — frontend-specialist task, non-trivial UI-only change.
- **NEW-5** (Wave 2 (major)) — /api/admin/* directories (sources, contact-positions, acquisition-channels, disconnect-reasons) accessible to manager role. Requires route-level gate changes in api.php (not in scope for this agent) or a shared middleware change.
- **minor-perf-indexes** (Wave 2 (major)) — Missing btree indexes on crm_contacts.owner_id and crm_contacts.created_by_id. Requires a new migration — no behavioral impact on correctness, pure performance improvement. Can be added as a standalone migration by backend-specialist.

### crm-companies
- **BLOCKER-1-2-list-export-visibility** (Wave 2 (major)) — Already fixed in Wave 1 via VisibilityResolver.applyScope() in CompanyService.list and CompanyExportService — confirmed in audit memory.
- **BLOCKER-3-merge-orphans** (Wave 2 (major)) — Already fixed in Wave 1 — DedupService.mergeCompany re-parents all 8 FK tables before soft-delete — confirmed in code review (lines 532-584).
- **MINOR-reconnect-idempotent** (Wave 2 (major)) — Minor severity — reconnect() already guards via unique_client_since → active vs prospect logic; the active→active log-write for repeated reconnect is a cosmetic issue. No FE button exists so the path is unreachable from UI.
- **MINOR-first-requisite-auto-set-current** (Wave 2 (major)) — Minor severity, not part of MAJOR scope. First/single requisite created as is_current=false leaves denorm null until manual set-current.
- **MINOR-company-channels-block-vue-warn** (Wave 2 (major)) — NEW-1 (FE bug: CompanyChannelsBlock not resolved in CompanyPage) — FE file ownership, needs frontend-specialist.

### catalog
- **minor-download-template-404** (Wave 2 (major)) — GET /api/catalog/price-import/template route does not exist. Fix requires a new route in api.php (owned by single-owner rule — cannot edit). Needs needsRoutes entry: GET catalog/price-import/template → PriceImportController@template (admin-write gate, returns sample .xlsx). Severity: minor (UX gap, no security risk).
- **minor-excel-import-amount-locale** (Wave 2 (major)) — PriceImportService reads amount via getFormattedValue()+(float) — locale-risky. Fix is internal to PriceImportService::processRow() — use getValue()/getCalculatedValue() instead. Deferred because: (a) no confirmed production incident; (b) import requires admin access. Can be done in a follow-up Catalog sprint.

### sales-deals
- **B0** (Wave 2 (major)) — Already fixed in Wave 1 (DealAmountCalculator folds discount_percent into deals.amount via recalcAmount + DealResource; update() re-runs recalcAmount on discount change unless amount_locked). Verified in code and via passing DealDiscountAndCompanyChangeTest — skipped.
- **M3** (Wave 2 (major)) — Already fixed in Wave 1: useDealsList.ts / useDealsBoard.ts now apply toKopecks(f.budget_from/to) before sending (x100), keeping the UI ' ₽' suffix. Verified — skipped.
- **M5** (Wave 2 (major)) — Already fixed in Wave 1: totalSumFormatted aggregates native amounts_by_currency per currency and formats via formatCurrency (no hardcoded ₽/млн/тыс literals); single currency = exact, all-rates = base with ≈, missing rate = per-currency breakdown. Verified — skipped.
- **m9** (Wave 2 (major)) — Stage-level visibility now ENFORCED in PipelineService::list (canAccessStage strips hidden stages from the funnel menu/stage editor payload) but the StageEditDrawer still has no FE editor for visible_department_ids/visible_user_ids. Building that editor is a frontend/designer task beyond surgical scope; left as a minor for frontend-specialist.
- **m6** (Wave 2 (major)) — Minor, dormant (no role maps to Department scope; all live deals have department_id=NULL). DealService::scopedQuery Department branch is unreachable today; deferring avoids touching an inert path. Lives in DealService — re-address when Department scope is activated.
- **m7/m10/m11/m12/t13/N2/N3** (Wave 2 (major)) — Minor/trivial FE polish and data-inconsistency items not in the assigned major set; several (N3 dealCreatedVerb i18n key, N2 unhandled 403) are already touched by parallel locale/DealPage edits in the working tree.

### sales-kpi
- **minor-dead-code-config-models-until-M10** (Wave 2 (major)) — CommissionRule/TeamTarget/SalaryPlan FK+status+currency are intentionally deferred to M10 per the S1.8 scope ('what is NOT included'). Marking them @deprecated or moving migrations to an M10 slice is out of this hardening pass and would churn migrations; left as-is.
- **minor-spec-drift-colleague-sums-for-privileged** (Wave 2 (major)) — Already correct in code: director/admin DO receive income_fact_kopecks per member (isPrivileged branch in buildTeamData, covered by test_team_director_sees_income_fact_kopecks_of_colleagues). No change needed; this is a vault spec-doc reconciliation, not a code defect.
- **trivial-spec-drift-income-fact-is-deals-not-payments** (Wave 2 (major)) — HD1 approximation (income = won deals by stage_changed_at) is documented and explicitly deferred to Finance M10 (first-payment + contract gating). No S1.8 action.
- **trivial-dead-literal-last_month-in-KpiPeriod** (Wave 2 (major)) — KpiPeriod type carries 'last_month'; backend KpiRequest still accepts it as a valid enum (reachable via raw URL), so removing the literal would narrow a still-valid API value. Cosmetic; left to avoid a type/contract mismatch.
- **minor-convention-authz-reads-users-role-not-spatie** (Wave 2 (major)) — Project-wide IAM-1 debt (enum users.role vs spatie on guard sanctum). New authz here goes through the service Gate-style helper consistent with existing AppServiceProvider gates (admin-write/dedup-scan-all all read $user->role). Switching to hasAnyRole is an IAM-1 migration, not a sales-kpi fix.

### sales-dashboard
- **blocker-xlsx-export** (Wave 2 (major)) — Per the brief this BLOCKER is already handled/denied and its fix lives outside sales-dashboard ownership: the FE blob-download via authed axios is in front/src/api/salesDashboard.ts + DashboardPage/index.vue exportXlsx (FE infra, not the aggregator), and the guest-401 (vs 500 'Route [login] not defined') belongs in bootstrap/app.php which I may not edit. No change required in my files.
- **minor-deals-without-tasks-period-scope** (Wave 2 (major)) — countDealsWithoutTasks (period/manager_id not threaded through) lives in App\Domain\Activity\Services\ActivityService — outside sales-dashboard file ownership. Left untouched; should be picked up by the Activity-domain agent.
- **minor-top-managers-group-by-full-name** (Wave 2 (major)) — Lower-severity (minor) and not in the assigned major set; group-by users.full_name collision + is_active filter — deferred to keep this change surgical to the three majors.

### activity
- **MAJOR-2** (Wave 2 (major)) — Admin CRUD for the meeting-report question registry has no FE UI (4 dead endpoints). Building the Settings «Справочники» editor page requires registering a new route in src/router (a shared file outside my ownership) plus a new full page. I added the FE api functions (listMeetingReportQuestions / createMeetingReportQuestion / updateMeetingReportQuestion / deleteMeetingReportQuestion + SaveMeetingReportQuestionPayload type) so the endpoints are now reachable; a follow-up agent only needs the router entry + the Settings page component. BE CRUD + policy (admin/director) already exist and are tested.
- **MINOR-8** (Wave 2 (major)) — Day boundaries in myBoard/applyPreset/reschedule are computed from UTC now() while config/app.php timezone='UTC' but the team is Asia/Dubai (+4h). A correct fix needs a shared config key (e.g. config/app.operational_timezone) consumed consistently; hardcoding 'Asia/Dubai' literals across three service methods would be a convention smell. config/* is off-limits — see needsShared.
- **MINOR-9** (Wave 2 (major)) — FTM 5-condition predicate is triplicated. The single-source query version (ActivityService::applyFtmConditions) is in my domain, but the two object-predicate copies (ManagerKpiService::ftmCounted and ActivityFeedItemResource::ftmCounted) live in the Sales domain (Domain/Sales/Services + Http/Resources/Sales), which is outside my file ownership. Non-verified convention-only minor.
- **MINOR-10** (Wave 2 (major)) — Unscoped responsible_id reassignment (exists:users,id only) is a non-verified security minor. Constraining the assignee to the actor's visibility scope + re-syncing the denormalised department_id on reassign is a reasonable change but was not in the Key-items list and carries reassignment-behaviour risk; deferring to keep this pass surgical and focused on the confirmed majors.

### inbox
- **M-3** (Wave 2 (major)) — Rate-limiter (RateLimiter::for('inbound')) lives in App\Providers\AppServiceProvider — a shared file outside my ownership. The channel_id:ip keying change is returned in needsShared with the exact replacement. Severity already downgraded major->minor by the audit (endpoints never run in prod yet; per-IP still gives flood protection).
- **M-4** (Wave 2 (major)) — findForDedup() phone full-scan lives in App\Domain\Crm\Services\CompanyService — crm-specialist's file, outside my scope. The structural fix (crm_companies.phone_normalized column + btree index, kept in sync on create/update) is ALREADY DONE; only the one-line query in findForDedup still needs to use it. Returned in needsShared. Severity downgraded major->minor by the audit (crm_companies = 13 rows, 0 with phones today).
- **admin-inbox-ui** (Wave 2 (major)) — Channels/Forms/Inbound-log admin UI (9 endpoints) is consciously deferred to the integrations sprint per the vault 'Что отложено' (approved 2026-06-12) and PLAN S1.9 §З — not a delivery bug, tracked as minor MISSING. Out of scope for this hardening pass.

### contracts-documents
- **MINOR-showApprovalInlineRoleCheck** (Wave 2 (major)) — showApproval uses inline in_array($user->role->value,...) instead of Policy. Convention violation but not a runtime bug — the endpoint has no FE caller. Requires adding ApprovalPolicy::view and $this->authorize in controller.
- **MINOR-approvalRoutesPageTemplateCode** (Wave 2 (major)) — ApprovalRoutesPage renders template_code column but ApprovalRouteResource does not expose it. FE shows empty column. Needs whenLoaded('template', fn() => $this->template->code) in ApprovalRouteResource and a template selector in the drawer.
- **MINOR-attachmentUploaderName** (Wave 2 (major)) — DocumentAttachmentResource does not expose uploader name or file size. Requires eager-loading uploadedBy relation and adding file size via Storage::disk('documents')->size($path).
- **MINOR-messageTemplatesPageRoleGuard** (Wave 2 (major)) — front/src/router/routes/base.ts restricts MessageTemplatesPage to admin/lawyer; BE MessageTemplatePolicy.viewAny allows director/manager. FE guard is over-restrictive.
- **MINOR-deadCodeDuplicate** (Wave 2 (major)) — DocumentPage/index.vue has a no-op Duplicate menu item; deleteMessageTemplate API function exists without UI; GET /message-templates/context has no FE caller. Requires either wiring or removing.

### onboarding
- **onboarding#7-minor** (Wave 2 (major)) — deadline_days from Course not applied to due_date in bulkAssign — AssignmentService::bulkAssign ignores course.deadline_days when due_date not explicitly passed. Low harm (null due_date, no overdue enforcement until cron). Fix: read course.deadline_days in bulkAssign and set due_date = today + deadline_days when not provided.
- **onboarding#8-minor** (Wave 2 (major)) — Certificate regenerate endpoint has no completion-guard — admin can issue a numbered cert on an incomplete assignment burning the sequence number. Fix: check assignment.status === completed in CertificateController::regenerate, return 422 otherwise.
- **onboarding#9-minor** (Wave 2 (major)) — CourseAssigned event dispatched by AssignmentService but has no registered listener — assignment notification never sent. Needs bot-specialist/automation-specialist to add a listener; not an onboarding-specialist concern.
- **onboarding#10-minor** (Wave 2 (major)) — AiTutorController ask/history endpoints lack authorize('view', lesson) — returns 200 empty for an unassigned lesson instead of 403. No data leak (sessions filtered by user_id) but incorrect status code.
- **onboarding#12-minor** (Wave 2 (major)) — N+1 progress calculation in MyCoursesResource and ProgressService::enrichRow — batch via grouped COUNT query when volume grows.

### notification
- **major-telegram-link-contract** (Wave 2 (major)) — Already fixed in a prior wave (likely the iam Telegram-link work the task flagged as overlapping). Verified: front/src/api/auth.ts reads {deeplink, expires_in_minutes} and useProfilePage opens res.deeplink; grep confirms zero 'link_url' references remain in front/src. Verified-and-skipped per instructions.
- **minor-id-oracle-single-read-403-vs-404** (Wave 2 (major)) — Acceptable for an internal CRM per the audit (ids not sensitive, body not leaked). Fix (scoped route binding) requires a route/binding change owned by the api.php single-owner agent; out of my file scope.
- **minor-id-oracle-readbatch-exists-global** (Wave 2 (major)) — Vault backlog explicitly accepts this as a non-blocker (service WHERE user_id already silently skips foreign ids). Tightening the exists rule is optional and the audit marks it minor; left as-is to avoid churn.
- **minor-spec-drift-vault-doc** (Wave 2 (major)) — Vault documentation drift (action_url, action|info categories, listener names). Code is canonical; this is a doc-only update owned by product-manager, not a code fix in my scope.

### log-shell
- **crm-feed-field-change-chip** (Wave 2 (major)) — Minor DEAD-CODE: CRM-feed 'Изменения' chip is mute because CrmFeedService only emits activity events; wiring a field_change source would require reading entity_logs DataChanged into the feed-merge (CrmFeedService is Crm-domain) and changing the CRM feed item normalisation in useEntityFeed.ts/EntityActivitiesTab.vue (Crm/Sales FE). Cross-owner and out of the explicit task scope (the task targets EntityLogTab/MiniTimeline + ChannelHistoryDrawer + contact-log + feed pagination). Left for crm-specialist.
- **post-commit-log-desync-deal-company** (Wave 2 (major)) — Minor DATA-INCONSISTENCY: DealService/DealMoveService/CompanyService record() after the parent commit (post-commit insert failure = 500 after a successful mutation). Fixing those means editing Sales/Crm domain services beyond ContactService; I made ContactService atomic (record inside the transaction) as the in-scope writer, but the deal/company writers are owned by sales/crm-specialist.
- **deal-feed-key-action-scroll** (Wave 2 (major)) — Minor DEAD-CODE: DealFeed.vue scrollToFeedItem maps kp_sent/contract_sent to field_change items that don't exist. DealFeed.vue is a Sales-page component (sales-specialist); rewiring the scroll target to the entity_logs source is out of my FE scope.
- **new1-companychannelsblock-resolve** (Wave 2 (major)) — NEW-1: '[Vue warn] Failed to resolve component CompanyChannelsBlock' on the company card — the component is used but not imported/registered in front/src/pages/CompanyPage/. That is a CompanyPage (crm) ownership area, not the log/timeline/channel-drawer components in my scope.
- **bell-only-in-orbita** (Wave 2 (major)) — UX shell issue: notification bell missing in sidebar nav mode. Belongs to the app-shell/header layer (DefaultLayout/AppSidebar), not the log-shell components I own.

### migration
- **minor #4 (SPEC-DRIFT) — config/amo_migration.php header documents an auto-map mechanism the code partly did not implement** (Wave 2 (major)) — config/* is outside my file ownership — returned in needsShared. The header now matches reality for products (migration_maps IS the product_option runtime cache, amo_product_mappings IS consulted). The remaining inaccuracy is that custom-field OPTION translation (beyond products) is still not implemented — a single comment tweak in config/amo_migration.php (lines 11-13) by the config owner.
- **minor #6 (STUB) — load/verify path not validated end-to-end against live AMO (external_refs=0)** (Wave 2 (major)) — Out of scope for a code fix and gated on a real cutover decision: requires AMO_MIGRATION_TOKEN (env, written by main), applying pending native-field migrations to prod, and a bounded smoke run against a staging PG. Per the project DEC 'перенос данных не нужен' the ETL is intentionally dormant; the wiring is now correct and fully unit-tested in SQLite, but a live run is a separate cutover task.
- **trivial #8 — no FE surface (console-only)** (Wave 2 (major)) — Architecturally correct for a temporary CLI-only bounded context dropped at M12; no fix needed.

### automation
- **MINOR-6** (Wave 2 (major)) — Per-run Retry endpoint + UI + not-releasing-slot-on-failed for inline runs. Requires a NEW route (out of my file ownership — api.php) and a controller method; returned in needsRoutes. The docstring claim and inline best-effort behaviour are pre-existing and not a correctness break.
- **minor-email-stub** (Wave 2 (major)) — email action is a deliberate forward-compatible no-op until the integrations sprint (vault §4.3/§12). Disabling/hiding the FE card is cosmetic and would diverge from the planned wiring; left as documented.
- **minor-policy-enum-vs-spatie** (Wave 2 (major)) — PipelineAutomationPolicy gating by users.role enum instead of spatie automation.manage is the project-wide IAM-1 debt (sanctum can't see spatie roles yet); every policy does this. Out of scope until the IAM-1 migration.
- **minor-automationsApi-get-dead-wrapper** (Wave 2 (major)) — automationsApi.get(id) / GET automations/{id} has no UI consumer; edit prefills from the cached list object. Harmless dead code; removing the route is out of my file ownership (api.php) and the wrapper removal is cosmetic.
- **minor-webhook-egress-allowlist** (Wave 2 (major)) — Admin-only + SSRF-guard already block private/loopback; a destination allowlist + egress audit is an integration-specialist concern (webhook infra is their boundary) and low-risk.

### iam
- **minor-sanctum-tokens-no-expiry** (Wave 3 (minor/trivial)) — config/sanctum.php is outside my edit scope (config/* excluded) and the audit itself downgraded this to a documented design decision (null TTL is the correct prod value for a Bearer SPA). A temp-2FA-token TTL change would be a product/security decision.
- **minor-resolve-visibility-dead-middleware** (Wave 3 (minor/trivial)) — ResolveVisibility middleware / VisibilityResolver / bootstrap registration are explicitly excluded from my scope; either consuming the attribute in query-scopes or removing the middleware needs the shared route/provider owner.
- **major-rbac-spatie-guard-drift** (Wave 3 (minor/trivial)) — Out of scope (config/sanctum.php, AppServiceProvider) and an open product question (connect spatie on sanctum guard vs drop permission tables). Not a minor/trivial item I can resolve surgically.
- **minor-department-crud** (Wave 3 (minor/trivial)) — Department read-only index is an intentional, vault-documented M0 skeleton (CRUD + org-chart + Department-visibility deferred to M1). Building it now is a product-scoped milestone decision, not a bug fix.
- **trivial-visibility-scope-department-unreachable** (Wave 3 (minor/trivial)) — VisibilityScope::Department is a documented M1 reserve; removing the dead branches vs keeping them is a product/roadmap decision and VisibilityResolver is out of my scope.
- **trivial-unused-password-reset-and-sessions-tables** (Wave 3 (minor/trivial)) — Dropping the sessions table / removing default Laravel skeleton tables is a destructive migration needing product confirmation (stateless Bearer API today, but web-session flow not formally ruled out); password_reset_tokens must stay until a reset feature exists.

### crm-contacts
- **minor-bug-position-filter** (Wave 3 (minor/trivial)) — Audit says 'seed crm_contact_positions, link via position_id, add dropdown'. ContactPosition table exists but is empty. Seeding the directory with real data requires a product/UX decision on what standard positions to seed. No logic bug to fix — the free-text filter works correctly, just matching against an empty table is useless. Defer to content/data phase.
- **minor-convention-dual-role** (Wave 3 (minor/trivial)) — users.role enum vs spatie model_has_roles dual source is a known structural debt (IAM-1). Resolving it requires migrating all authz to spatie-on-Sanctum across the entire codebase. Out of scope for a single domain wave-3 pass.
- **trivial-stub-empty-tables** (Wave 3 (minor/trivial)) — contact_channels, crm_contact_relations, crm_contact_positions have 0 rows in live DB. These are code-complete features that simply have not been used yet. The fix is real data / QA with real users, not a code change.
- **trivial-spec-drift** (Wave 3 (minor/trivial)) — Vault spec for Contact omits created_by_id, acquisition_channel_id, last_activity_at. Vault is documentation, not code. Updating it is out of scope for a code-fix pass.
- **new5-admin-directories-accessible-to-manager** (Wave 3 (minor/trivial)) — NEW-5: /api/admin/* directories (sources, contact-positions, acquisition-channels, disconnect-reasons) return 200 for manager. This is an auth-gate issue on the admin routes — backend-specialist scope (auth/permissions layer). Changing those route guards is outside the crm-contacts file ownership boundary.

### crm-companies
- **minor-security-admin-dirs-read-gate** (Wave 3 (minor/trivial)) — NEW-5: /api/admin/* index/show endpoints (company-types, sources, countries, cities, contact-positions, acquisition-channels, disconnect-reasons) are readable by any authenticated user. The audit notes these directories 'feed filters' and read-any may be intentional. Adding a gate would break filter dropdowns for managers. Needs a product decision on which directories (if any) warrant a read gate. Specifically acquisition-channels and disconnect-reasons are flagged as more sensitive — but blocking their read breaks the disconnect dialog dropdown. Deferred pending product decision.
- **minor-dead-patch-channel-endpoint** (Wave 3 (minor/trivial)) — PATCH /api/companies/{company}/channels/{channel} endpoint exists on BE (CompanyChannelController::update) and companiesApi.updateChannel() is defined in FE but the channel menu only offers Delete — no edit action. The component (CompanyChannelsBlock) offers no UI path to call updateChannel. Wiring up an edit-in-place form for channels is a FE feature addition beyond minor fix scope; the dead method/endpoint is harmless. Deferred as future FE feature work.
- **minor-double-role-source** (Wave 3 (minor/trivial)) — CompanyPolicy/AppServiceProvider Gates read users.role column while VisibilityResolver prefers spatie getRoleNames(). This is tracked as IAM-1 (full migration to spatie-on-Sanctum). Divergence is non-exploitable in-app (UserService writes both atomically) and fixing it requires IAM-1 landing first. Not touched to avoid premature migration.

### catalog
- **catalog-trivial-scheduler-time** (Wave 3 (minor/trivial)) — Scheduler dailyAt('03:00') in console.php vs vault docs saying 00:05 UTC. Code is already self-consistent (comment on line 28 says '03:00 UTC'). Vault is stale — update belongs to PM/docs, not a code change. No functional impact.

### sales-deals
- **m7** (Wave 3 (minor/trivial)) — Saved views for deals require adding case Deal to SavedViewEntity (app/Domain/Crm/Enums/SavedViewEntity.php — crm-specialist's domain) plus a FE control. Cross-domain enum change + a new feature, not a minor in-scope fix.
- **m9** (Wave 3 (minor/trivial)) — Stage-visibility backend write + enforcement is already done (Wave 2, covered by PipelineVisibilityTest). The only remaining piece is a FE visibility editor in StageEditDrawer (role/department/user selectors), which is a product UX/design decision (which roles/departments to expose, pipeline vs stage presentation) — should go through designer first.
- **m11** (Wave 3 (minor/trivial)) — Not a code defect. deal_stage_history '1 row / 13 deals' is a live-data observation: the creation row IS written by DealService::create (from_stage_id=null) and DealMoveService records moves; metricsFor correctly excludes the creation row. The sparse history reflects that seeded/imported deals weren't moved much, not a missing write.

### sales-kpi
- **trivial-kpiperiod-last-month-literal** (Wave 3 (minor/trivial)) — FE type `KpiPeriod = 'current_month' | 'last_month' | string`. The `string` member already absorbs every value, so the literals are purely documentary and removing `'last_month'` has ZERO compile-time effect. The backend (KpiRequest/KpiFilters) genuinely accepts `last_month`, so the literal correctly documents an accepted value. Removing it is pure churn with no benefit; deferring to avoid noise.
- **minor-major-already-closed-waves-1-2** (Wave 3 (minor/trivial)) — The two MAJOR items (no role-gate on /api/me/*; team-comparison dead via NULL department_id) and most minors (score_pct ignores plan currency; alone-state unreachable; team-query role=Manager-only; cross-user picker stub; raw lowercase target token; colleague sums not emitted for director/admin; client-side badge dup) were ALL already closed in Waves 1-2 — verified in ManagerKpiService (assertCanViewCabinet, normalisedPlanKopecks, resolveTeamMemberIds, per-member score_badge, privileged income_fact_kopecks), ActivityFeedList (i18n labels + router-link + locale.value), TeamComparisonTable (size<=1 branch), and the wired cross-user picker (composable + index.vue Select). 70 pre-existing tests cover them. Nothing to redo.
- **minor-convention-spatie-vs-enum-role** (Wave 3 (minor/trivial)) — Authz reads enum users.role (Role::Admin/Director) rather than spatie. This is the project-wide IAM-1 debt (spatie-on-Sanctum not yet wired — spatie perms live on guard `web`, Sanctum can't see them). Migrating just this surface to hasAnyRole would diverge from every other domain and could silently 403 users whose spatie role and users.role disagree. This is a coordinated IAM-1 change, not a per-domain minor — defer to the IAM-1 migration.
- **trivial-spec-drift-income-fact-payments** (Wave 3 (minor/trivial)) — income_fact = SUM(won deals.amount) is the documented HD1 approximation; switching to first-payment requires the Finance module (M10) and is explicitly out of S1.8 scope. No action possible now.

### sales-dashboard
- **minor-deals-without-tasks-period-manager-scope** (Wave 3 (minor/trivial)) — The fix requires changing Activity-domain ActivityService::countDealsWithoutTasks(pipelineId, user) to also accept period + manager_id (or a scoped base query). ActivityService lives in app/Domain/Activity which is outside my granted file scope (app/Domain/Sales/** + DashboardPage). Listed in needsShared.
- **minor-avg-days-deal-stage-history-spec-drift** (Wave 3 (minor/trivial)) — Funnel avg_days = elapsed-since-move rather than true dwell-time; implementing real dwell-time requires reading deal_stage_history (a non-trivial rework, not a minor surgical change) or a vault spec update. Audit marks it spec-drift; a product/spec decision is needed on which way to reconcile.
- **minor-trial-bucket-threshold-overlap** (Wave 3 (minor/trivial)) — The HOT/Warm/Trial probability buckets overlap (keyword 'trial'=0.5 lands in Warm). The thresholds and probability_keywords live in config/crm.php which is outside my file scope, and the vault explicitly marks this known/accepted to be revisited at M10. Needs a product decision on renaming buckets vs shifting the trial probability.
- **major-default-pipeline-preselect-active-first** (Wave 3 (minor/trivial)) — FINAL severity is major (out of this wave's minor+trivial scope) and the audit downgraded it to a data-state issue. A clean code fix needs PipelineResource/PipelineDto to expose is_active so the FE can pre-select the first active pipeline; that touches the shared Pipeline resource shape and is better handled as a deliberate major item.
- **trivial-team-targets-not-connected** (Wave 3 (minor/trivial)) — team_targets (plan side) is correctly out of S1.7 dashboard scope per vault §А (plan/fact lives in S1.8/M10). No action needed for the dashboard; not a defect.

### activity
- **MINOR-9** (Wave 3 (minor/trivial)) — FTM-predicate triplication: the query single-source (ActivityService::applyFtmConditions) already exists, but the two object-predicate copies live in Sales-domain files (ManagerKpiService::ftmCounted, ActivityFeedItemResource::ftmCounted) which are outside this task's file ownership (app/Domain/Activity/**, Http/Controllers/Activity/**, listed FE files only). They also operate on plain objects/models rather than a query builder, so a clean single-source needs a shared static predicate consumed by all three — that edit would touch the two Sales files. Defer to a sales-domain pass.
- **MINOR-7** (Wave 3 (minor/trivial)) — Dead reschedule route: now non-dead at the contract level — POST /api/activities/{id}/reschedule is implemented + tested and front/src/api/activity.ts exports rescheduleActivity (timezone-correct preset). What remains is wiring a one-click quick-reschedule button on a task row/card; that is a UX/placement decision (designer + frontend-specialist). TaskQuickForm's applyDateShortcut is a form-field picker, not a one-click reschedule, and converting it would change semantics.
- **MINOR-11** (Wave 3 (minor/trivial)) — STUB (meeting-report/FTM cold in prod): not a code fix — requires running the now-revived meeting-report + FTM flow end-to-end against the live app (and exercising company-target + standalone modes). The structural defects were closed in Wave-3; this is a verification/QA task.

### inbox
- **M-3** (Wave 3 (minor/trivial)) — SECURITY minor — rate-limiter keyed channel_id:ip per spec E5. Fix lives in AppServiceProvider::boot() RateLimiter::for('inbound') registration, an explicitly forbidden file. Returned in needsShared instead.
- **M-4** (Wave 3 (minor/trivial)) — PERF minor — findForDedup phone-path full-table scan. Method is in app/Domain/Crm/Services/CompanyService (crm-specialist's domain), outside my file ownership. phone_normalized column + index already exist; only the findForDedup query still does the PHP scan. Returned in needsShared.
- **minor-MISSING-admin-inbox-ui** (Wave 3 (minor/trivial)) — Admin channels/forms/inbound-log CRUD UI. Explicitly deferred to the integrations sprint per vault decision (Q3, approved 2026-06-12). Sprint/product decision, not a delivery bug — building it now would exceed scope.
- **minor-MISSING-failed-routing-viewer** (Wave 3 (minor/trivial)) — No FE viewer for GET /api/inbox routing_status (operators can't see why an intake failed). Part of the same deferred admin inbox UI sprint; backend GET /api/inbox is already built and tested.
- **rbac-note-channel-viewany-open** (Wave 3 (minor/trivial)) — ChannelPolicy viewAny/view open to any authenticated user. Audit flags this only as a low-risk NOTE (no FE, masked token) with no proposedFix; the policy docblock documents it as intentional (assign/config dropdowns). Left as-is to avoid churn on a documented design.

### contracts-documents
- **minor-dual-role-source** (Wave 3 (minor/trivial)) — users.role (string mirror) + spatie roles/model_has_roles both present; policies read the mirror. Fixing requires system-wide IAM-1 migration to spatie-on-Sanctum — cross-domain, not safe to change in this wave.
- **minor-create-document-dialog-hardcoded-products-countries** (Wave 3 (minor/trivial)) — CreateDocumentDialog hardcodes productOptions and countryOptions instead of fetching from /api/admin/products and /api/admin/countries. Requires careful FE API integration (field mapping for countries catalog) and product code alignment — non-trivial, risky for a minor fix.

### onboarding
- **#9** (Wave 3 (minor/trivial)) — CourseAssigned event has no listener — intentional per docblock ('bot-specialist M11: Telegram notification subscribes here'). No action until bot-specialist sprint wires the listener. The event dispatch is correct and the event class is ready.
- **#11** (Wave 3 (minor/trivial)) — CompletionPolicy::SoftGate and AssignmentStatus::Failed are design placeholders documented as 'stored but not enforced' in enum docblocks. Enforcement requires a workflow-integration product decision (what gets blocked when SoftGate fires). Deferred to a dedicated sprint.
- **#16** (Wave 3 (minor/trivial)) — AssignCourseDrawer fetches /api/users without role-scoping (trivial). Fix requires a new scoped endpoint (/api/users/assignable) which touches routes/api.php and is backend-specialist territory. Small blast radius (assigning course to an admin role user does not grant them student permissions). Deferred to backlog.

### notification
- **notification-minor-spec-drift-vault** (Wave 3 (minor/trivial)) — Pure documentation update to the Obsidian vault doc '2. Модули/Notification — Telegram-бот.md' (action_url->deep_link, 2-value->5-value enum, listener/event names, response shape). It is a vault doc outside the code repo and is PM-owned per the audit §7 workflow — not a src/front code fix. No code drift: code is canonical and FE already matches it.
- **notification-major-1-and-2-and-other-minor-trivial** (Wave 3 (minor/trivial)) — Already closed in Waves 1-2 (verified, not redone): (a) telegram-link contract uses res.deeplink in auth.ts + useProfilePage; (b) bell now mounted in sidebar via SidebarNotifications.vue + store startPolling(); (c) badge polling added (notificationsStore.startPolling, 60s, pauses on hidden tab); (d) telegram @+chat_id replaced by profile.telegram.accountId i18n key (ru+en, no @); (e) telegram_user_id overwrite guard via LinkRedeemResult::AlreadyLinkedOther (test-covered); (f) digest.by_category cast to (object) -> {} when empty (test-covered); (g) mark-all-read now consumes res.unread_count via syncCount in useNotificationsFlyout + typed ReadAllResponse in notifications.ts/entities.

### log-shell
- **minor-DEAD-CODE-DealFeed-scroll-kp-contract** (Wave 3 (minor/trivial)) — DealFeed.vue lives in pages/DealPage (sales-domain feed) and scrollToFeedItem maps kp_sent/contract_sent to type==='field_change', but those are entity_logs actions never present in the deal-feed merge (which only carries deal_audits field_change). Fixing requires adding a new deal-feed source/type — sales-feed semantics, ambiguous source, and outside my file scope (EntityLogTab/MiniTimeline/ChannelHistoryDrawer + shells). Hand to sales-specialist.
- **minor-DEAD-CODE+product-ContactService-log-coverage** (Wave 3 (minor/trivial)) — Audit explicitly flags this as an OPEN PRODUCT QUESTION: whether to add field-change logging to ContactService::create/update or remove the contact-log surface. Needs a product decision, not a code fix.
- **minor-DATA-INCONSISTENCY-entity-logs-backfill** (Wave 3 (minor/trivial)) — Audit states backfill is NOT needed (test data, no migration planned). Company/contact write-paths now exercised by the new CrmFeed test + existing EntityLogRecordingTest; nothing to fix.
- **minor-SPEC-DRIFT-vault-log-spec** (Wave 3 (minor/trivial)) — Doc-only drift in the Obsidian vault (dev-brain, not shipped code) with zero runtime/security effect. Vault edits are out of the src/front code scope; owner is product-manager/vault.
- **shell-UX-bell-only-in-orbita** (Wave 3 (minor/trivial)) — Notification bell missing from sidebar nav mode needs a design decision on where to host the bell trigger in the shared header layer (which header slot). Ambiguous placement — needs designer/product input before implementing.

### migration
- **#4 config comment SPEC-DRIFT** (Wave 3 (minor/trivial)) — Fix lives in config/amo_migration.php (lines 8-13 header comment) which is OUT of my file ownership (config/*). Returned as needsShared. Note: the comment is now technically accurate since migration_maps + amo_product_mappings ARE read by ProductLineResolver (wired in Waves 1-2), so this is a wording-refinement, not a real drift anymore.
- **#6 STUB end-to-end not run** (Wave 3 (minor/trivial)) — Operational/cutover go/no-go item, not a code fix. Validating load/verify end-to-end requires a real AMO_MIGRATION_TOKEN, a populated staging dir, and a real Postgres staging DB — none exist in dev (external_refs=0). Aligns with the project DEC 'data transfer not needed (test data)'. Needs a product/cutover decision, not a surgical edit.
- **#8 TRIVIAL no FE surface** (Wave 3 (minor/trivial)) — Architecturally intentional: the migration domain is a temporary console-only bounded context (no routes/controllers/Resources by design, dropped at M12). Adding an in-app cutover console would require new BE HTTP routes + admin Policy + a Vue page/store — large net-new surface for a domain slated for removal. Confirmed by audit + live-QA (zero AMO journey in SPA).

### automation
- **per-run-retry-endpoint** (Wave 3 (minor/trivial)) — MINOR-6: adding a per-run Retry needs a new route in routes/api.php (not in my file ownership) plus a design decision on AutomationEngine.finalize NOT freeing the failed slot so retry is idempotent — a core lifecycle change, too risky for a minor pass. Listed in needsRoutes.
- **policy-enum-vs-spatie** (Wave 3 (minor/trivial)) — CONVENTION minor: PipelineAutomationPolicy gates on the users.role enum rather than the seeded automation.manage ability. This is the project-wide IAM-1 debt (spatie not yet on the Sanctum guard); migrating it domain-by-domain would diverge from every other policy and is owned by the IAM-1 migration, not this audit pass.
- **webhook-egress-allowlist** (Wave 3 (minor/trivial)) — SECURITY minor (admin-only + SSRF guard + 0 live usage): a destination allowlist + egress audit is a product/security decision and lives in config/automation.php (not in my file ownership). Needs a product call on whether to restrict outbound hosts.
- **change_owner-positional-cursor-instability** (Wave 3 (minor/trivial)) — The round_robin cursor is a single positional integer ordered by id; adding/removing/deactivating a pool member shifts all subsequent assignments. Keying rotation to a stable per-user set changes the rotation contract and dry-run/preview semantics — too behavioural for a minor fix; the no-op-skip half was safe and is done.
